export const goToPokePage = (navigate) => {
  navigate("/");
};

export const goToPokeDetail = (navigate) => {
  navigate("/PokeDetail");
};

export const goToPokeList = (navigate) => {
  navigate("/PokeList");
};
